import FilterBar from './filterBar';
import SortBar from './sortBar';
import ActionBar from './actionBar';

class KBars {
  FilterBar = FilterBar;

  SortBar = SortBar;

  ActionBar = ActionBar;
}

export default new KBars();
